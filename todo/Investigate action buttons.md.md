In a real client, the different actions are not triggered on request by button but instead are a consequence of accessing a protected resource without a valid session.

Depending on the direction we'll take for the solution, we might want to remove the individual action buttons.