{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Client: Launch in browser",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5000",
      "webRoot": "${workspaceFolder}/Client",
      "preLaunchTask": "Client: dev server"
    }
  ]
}
